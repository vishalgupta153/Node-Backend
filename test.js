//1. unit under test
import Auth from './api/v1/controller/auth'
describe('login', () => {
  describe('Auth', () => {
    describe('Auth.login', (err, succ) => {
      console.log('err,succ', err, succ)

    })
  })
});